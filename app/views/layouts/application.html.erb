<!DOCTYPE html>
<html>
<head>
  <title>PET DAY</title>
  <link rel="shortcut icon" type="image/x-icon" href="path/to/cat-icon.ico">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <script src="https://kit.fontawesome.com/853b12f8a1.js" crossorigin="anonymous"></script>
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  <style>
    /* ボタンのデフォルトスタイル */
    .nav-link {
      transition: background-color 0.3s ease;
      font-family: 'Pop', sans-serif;
      font-size: 20px;
      line-height: 40px;
      box-shadow: 0px 4px 5px rgba(0, 0, 0, 0.1);
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
    }

    /* ボタンのホバー時のスタイル */
    .nav-link:hover {
      background-color: white;
    }

    /* ナビゲーションバーのブランド名のスタイル */
    .navbar-brand span {
      font-family: 'Indie Flower', cursive;
      font-size: 3.5rem;
      font-weight: bold;
      text-shadow: 2px 2px 5px white;
    }

    .header-bg {
      background-image: url('/assets/696668.jpg');
      background-size: cover;
      background-position: center;
      height: 150px; /* ヘッダーの高さを固定値に設定 */
    }
  
    
  </style>
</head>
<body class="d-flex flex-column vh-100">
  <header class="shadow-sm header-bg">
    <nav class="navbar navbar-expand-lg navbar-light">
      <div class="container">
        <%= link_to "/", class: 'navbar-brand' do %>
          <span>PET DAY</span>
        <% end %>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">
            <% if user_signed_in? %>              
              <li class="nav-item">
                <%= link_to "マイページ", public_user_path(id: current_user.id), method: :get, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "投稿一覧", public_posts_path, method: :get, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "新規投稿", new_public_post_path, method: :get, class: 'nav-link text-dark' %>
              </li>            
              <li class="nav-item">
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: 'nav-link text-dark' %>
              </li>
            <% elsif admin_signed_in? %>
              <li class="nav-item">
                <%= link_to "投稿一覧", admin_posts_path, method: :get, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "ユーザー一覧", admin_users_path, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'nav-link text-dark' %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "投稿一覧", public_posts_path, method: :get, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "新規登録", new_user_registration_path, method: :get, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "ログイン", new_user_session_path, method: :get, class: 'nav-link text-dark' %>
              </li>
              <li class="nav-item">
                <%= link_to "管理者ログイン", new_admin_session_path, method: :get, class: 'nav-link text-dark' %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <main>
    <%= yield %>
  </main>
</body>
</html>
