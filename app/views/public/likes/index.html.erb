<style>
  @media only screen and (max-width: 600px) {
    /* ここにCSSルールを追加 */
  }
</style>



<h1>あなたがいいねした投稿一覧</h1>

<% if @likes.any? %>
  <ul>
    <% @likes.each do |like| %>
      <li>
        <% if like.post.image.attached? %>
          <%= link_to image_tag(like.post.image.variant(resize: "100x100")), public_post_path(like.post) %>
        <% else %>
          <%= link_to image_tag("no_image.jpg", width: 100, height: 100), public_post_path(like.post) %>
        <% end %>
        <%= link_to like.post.user.name, public_user_path(like.post.user) %>
        <%= link_to like.post.title, public_post_path(like.post) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>いいねした投稿はありません。</p>
<% end %>


<% if @likes.present? && @likes.any? %>
  <%= will_paginate @likes %>
<% end %>
<%= link_to "Back", public_user_path(id: current_user.id), class: "btn btn-secondary" %>
