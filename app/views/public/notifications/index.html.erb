<h1>通知一覧</h1>

<% if @notifications.present? %>
  <ul>
    <% @notifications.each do |notification| %>
      <li><%= notification.title %></li>
    <% end %>
  </ul>
<% else %>
  <p>通知はありません。</p>
<% end %>